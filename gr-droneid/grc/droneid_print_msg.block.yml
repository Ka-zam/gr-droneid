id: droneid_print_msg
label: Print droneid
category: '[Droneid]'

templates:
  imports: from gnuradio import droneid
  make: droneid.print_msg()

inputs:
- domain: message
  id: in
  optional: true
#  'file_format' specifies the version of the GRC yml format used in the file
#  and should usually not be changed.
file_format: 1
